# Система ветра и погоды

## Описание изменений

Добавлена комплексная система ветра и погоды в игре "Пчелка Майя". Система включает в себя случайный ветер, три типа погоды и их влияние на игровые элементы. Ветер влияет на раскачивание деревьев и полет пчел, а тип погоды влияет на количество облаков и активность пчел. Информация о ветре и погоде отображается в информационной панели игры.

## Основные функции системы ветра и погоды

### Генерация ветра
- **Сила ветра**: случайное значение от 1 до 10
- **Направления**: С, С/З, С/В, Ю, Ю/З, Ю/В
- **Постоянство**: ветер остается неизменным в течение всего дня
- **Обновление**: при переходе к следующему дню генерируется новый ветер

### Логика направлений ветра
- **Северные направления** (С, С/З, С/В): ветер дует справа налево
- **Южные направления** (Ю, Ю/З, Ю/В): ветер дует слева направо

### Типы погоды
#### 1. Ясно (☀️)
- **Вероятность**: 60%
- **Облака**: базовое количество (3 облака)
- **Пчелы**: нормальная активность
- **Влияние ветра**: стандартное

#### 2. Пасмурно (☁️)
- **Вероятность**: 30%
- **Облака**: в 5 раз больше (15 облаков)
- **Пчелы**: сниженная активность (в 3 раза меньше)
- **Влияние ветра**: стандартное

#### 3. Дождь (🌧️)
- **Вероятность**: 10%
- **Облака**: в 10 раз больше (30 облаков)
- **Пчелы**: полностью прекращают полет
- **Визуальные эффекты**: эффект дождя с каплями и рябью на земле
- **Влияние ветра**: стандартное

## Созданные файлы

### 1. static/js/weather.js
**Основной JavaScript файл системы ветра и погоды**

**Ключевые функции:**
- `weatherSystem.initialize()` - инициализация системы ветра и погоды
- `generateDailyWind()` - генерация случайного ветра на день
- `generateDailyWeather()` - генерация случайного типа погоды на день
- `updateWeatherDisplay()` - обновление отображения ветра и погоды в интерфейсе
- `applyWeatherEffects()` - применение всех эффектов погоды
- `applyWindToTrees()` - применение ветра к анимациям деревьев
- `applyWindToBees()` - применение ветра к системе полета пчел
- `applyWeatherToClouds()` - применение погоды к количеству облаков
- `applyWeatherToBees()` - применение погоды к активности пчел
- `getWeatherIcon()` - получение иконки для типа погоды
- `getCloudMultiplier()` - получение множителя количества облаков
- `getBeeActivity()` - получение информации о активности пчел
- `createRainEffect()` - создание эффекта дождя с каплями и рябью
- `removeRainEffect()` - удаление эффекта дождя
- `applyWeatherVisualEffects()` - применение визуальных эффектов погоды

**Система управления ветром:**
```javascript
let weatherSystem = {
    wind: {
        force: 0,           // Сила ветра (1-10)
        direction: '',      // Направление ветра
        directionGroup: '', // Группа направлений
        isActive: false     // Активен ли ветер
    }
};
```

### 2. static/css/weather.css
**CSS стили для визуальных эффектов ветра и погоды**

**Основные стили для ветра:**
- `.wind-affected` - базовые эффекты ветра на элементы
- `.wind-force-{1-10}` - стили для разных уровней силы ветра
- `.wind-info` - стили для отображения информации о ветре
- CSS переменные для динамических эффектов (`--wind-intensity`)

**Стили для погоды:**
- `.weather-info` - стили для отображения информации о погоде
- `.weather-sunny` - стили для ясной погоды
- `.weather-cloudy` - стили для пасмурной погоды
- `.weather-rainy` - стили для дождливой погоды
- `.cloud-extra` - стили для дополнительных облаков

**Стили для эффекта дождя:**
- `.rain-effect` - контейнер для эффекта дождя
- `.raindrop` - стили для капель дождя
- `@keyframes rainfall` - анимация падения капель
- `.rain-ripple` - стили для эффекта ряби на земле
- `@keyframes ripple-effect` - анимация ряби

**Визуальные эффекты:**
- Изменение яркости и контрастности в зависимости от силы ветра
- Цветовое кодирование силы ветра и типа погоды в интерфейсе
- Эффекты дрожания и наклона для пчел
- Усиление анимаций раскачивания деревьев
- Стили для дополнительных облаков при пасмурной погоде и дожде

## Изменения в существующих файлах

### 1. templates/base.html
**Добавлено подключение новых файлов:**
```html
<link rel="stylesheet" href="{{ url_for('static', filename='css/weather.css') }}">
<script src="{{ url_for('static', filename='js/weather.js') }}"></script>
```

**Изменения в информационной панели:**
- В `<div class="game-info">` автоматически добавляется информация о ветре
- Формат отображения: `Ветер: ↘ С 7/10`
- Интерактивная подсказка с детальной информацией

### 2. static/js/main.js
**Измененные функции:**

#### flyToTarget(bee)
- Добавлено смещение траектории полета в зависимости от направления ветра
- Ветер замедляет скорость полета пчел
- Добавлен визуальный наклон пчел от ветра

#### spawnBee()
- Добавлена интеграция с системой ветра при создании пчел
- Пчелы получают начальное смещение от ветра
- Добавлено логирование с информацией о ветре

#### applyVisualEffect(beeElement)
- Добавлены классы `wind-mode` для пчел при ветре
- CSS переменные для динамических эффектов ветра
- Наклон пчел в зависимости от направления ветра

#### updateAllBeesVisualEffects()
- Добавлено дрожание пчел от ветра
- Динамические тени с эффектом ветра
- Логирование состояния ветра

#### initializeBeeFlightSystem()
- Применение эффектов ветра при инициализации системы пчел

#### nextDay()
- Обновление системы ветра при переходе к следующему дню
- Генерация нового ветра для нового дня

#### updateSeasonalTrees()
- Применение эффектов ветра к деревьям после обновления сезонов

## Влияние ветра и погоды на игровые элементы

### Деревья
- **Раскачивание**: усиливается в зависимости от силы ветра
- **Анимация**: изменяется скорость и амплитуда раскачивания
- **Расссинхронизация**: при сильном ветре (6+) деревья качаются с разными фазами
- **Визуальные эффекты**: изменение яркости, контрастности, насыщенности

### Пчелы
- **Влияние ветра**:
  - Количество: чем сильнее ветер, тем меньше пчел вылетает
  - Базовое количество: 20 пчел
  - Снижение: 1.5 пчелы за каждый балл ветра
  - Минимум: 5 пчел при очень сильном ветре
  - Траектория полета: пчелы смещаются в сторону ветра
  - Скорость полета: сильный ветер замедляет пчел
  - Визуальные эффекты: наклон, дрожание, тени

- **Влияние погоды**:
  - Ясно: нормальная активность пчел
  - Пасмурно: активность снижена в 3 раза
  - Дождь: пчелы полностью прекращают полет

### Облака
- **Влияние ветра**: изменение скорости движения
  - При сильном ветре: движение до 5 раз быстрее
  - При слабом ветре: движение медленнее
- **Влияние погоды**: изменение количества
  - Ясно: 3 базовых облака
  - Пасмурно: 15 облаков (в 5 раз больше)
  - Дождь: 30 облаков (в 10 раз больше)
- **Визуальные эффекты дождя**:
  - Капли дождя падают с неба (100 капель)
  - Эффект ряби на земле
  - Реалистичная анимация дождя

### Интерфейс
- **Отображение**: информация о ветре и погоде в `<div class="game-info">`
- **Ветер**:
  - Слабый ветер (1-3): зеленый
  - Умеренный ветер (4-6): желтый
  - Сильный ветер (7-8): оранжевый
  - Очень сильный ветер (9-10): красный
- **Погода**:
  - Ясно (☀️): золотистый с тенью
  - Пасмурно (☁️): серый
  - Дождь (🌧️): синий

## Технические детали

### CSS переменные
- `--wind-intensity` - интенсивность ветра (0-1)
- `--wind-tilt` - угол наклона от ветра
- `--sway-amplitude` - амплитуда раскачивания
- `--sway-offset` - смещение от ветра

### JavaScript интеграция
- Система ветра инициализируется при загрузке DOM
- Автоматическое обновление при переходе между днями
- Интеграция с существующей системой сезонов
- Совместимость с системой затмения солнца

### Производительность
- Эффекты ветра применяются только при изменении погоды
- Использование CSS переменных для плавных переходов
- Оптимизированные вычисления траектории пчел

## Тестирование

### Проверка функциональности
1. **Генерация ветра**: проверить случайную генерацию силы и направления
2. **Отображение**: убедиться, что информация о ветре появляется в интерфейсе
3. **Деревья**: проверить усиление раскачивания при увеличении ветра
4. **Пчелы**: убедиться в изменении количества и траектории полета
5. **Обновление дня**: проверить изменение ветра при переходе к следующему дню

### Отладка
- Подробное логирование в консоли браузера
- Визуальные индикаторы состояния ветра
- Проверка CSS классов и переменных

## Совместимость

### Браузеры
- Поддержка всех современных браузеров
- Использование стандартных CSS и JavaScript возможностей
- Адаптивность для мобильных устройств

### Производительность
- Минимальное влияние на FPS игры
- Эффективное использование памяти
- Плавные анимации без рывков

## Будущие улучшения

### Возможные расширения
- Добавление звуковых эффектов ветра
- Влияние ветра на другие игровые элементы (листья, снег)
- Более сложная физика взаимодействия ветра с объектами
- Сохранение данных о ветре в базе данных
- Влияние ветра на экономические аспекты игры

---

**Статус**: ✅ Реализовано  
**Версия**: v.0.0.1  
**Дата**: 2024  
**Автор**: KodaCode