# Система полета пчел - Документация

## Обзор системы

Реализована продвинутая система полета пчел в игре "Пчелка Майя", которая моделирует реалистичное поведение пчел в весенне-летний период. Система полностью заменяет простые анимации пчел на интерактивную систему с умным поведением.

## Алгоритм полета пчел

### Основные принципы

1. **Вылет из улья**: Пчелы вылетают из улья со скоростью:
   - **3 пчелы в секунду** - если солнце не затенено облаком
   - **1 пчела в секунду** - если солнце затенено облаком

2. **Скорость полета**: Скорость полета пчел также зависит от освещенности:
   - **150% от базовой скорости** - при ярком солнце (быстрый полет)
   - **50% от базовой скорости** - при затмении (медленный полет)

3. **Случайная траектория**: Пчелы летают по случайной траектории в пределах игрового поля

4. **Обнаружение цветков**: Если на расстоянии **2-5 пикселей** находится цветок, пчела:
   - Садится на него
   - Ждет **2-5 секунд**
   - Меняет свой цвет на цвет цветка и сохраняет его
   - Летит дальше с новым цветом

5. **Возвращение в улей**: После посещения **3-5 цветков** пчела возвращается в улей

## Техническая реализация

### Архитектура системы

```javascript
beeFlightSystem = {
    bees: [],                    // Массив активных пчел
    isActive: false,             // Активность системы
    eclipseActive: false,        // Состояние затмения солнца
    lastSpawnTime: 0,            // Время последнего спавна
    spawnInterval: 333,          // Интервал спавна в миллисекундах
    maxBees: 20,                 // Максимальное количество пчел
    baseFlightSpeed: 2,          // Базовая скорость полета
    eclipseSpeedMultiplier: 0.5, // Множитель скорости при затмении (50%)
    brightSunSpeedMultiplier: 1.5, // Множитель скорости при ярком солнце (150%)
    currentSpeedMultiplier: 1.5  // Текущий множитель скорости
}
```

### Состояния пчелы

Каждая пчела может находиться в одном из трех состояний:

1. **`flying`** - пчела летит к случайной цели или к цветку
2. **`sitting`** - пчела сидит на цветке и ожидает
3. **`returning`** - пчела возвращается в улей

### Структура данных пчелы

```javascript
{
    id: "уникальный_идентификатор",
    element: "DOM_элемент_пчелы",
    state: "flying|sitting|returning",
    hiveX: "x_координата_улья",
    hiveY: "y_координата_улья", 
    x: "текущая_x_позиция",
    y: "текущая_y_позиция",
    targetX: "целевая_x_позиция",
    targetY: "целевая_y_позиция",
    speed: "скорость_полета",
    visitedFlowers: "количество_посещенных_цветков",
    maxFlowers: "максимум_цветков_для_посещения",
    currentFlower: "текущий_цветок",
    sitTimer: "таймер_ожидания",
    color: "текущий_цвет_пчелы (меняется на цвет посещенного цветка)"
}
```

## Основные функции системы

### Инициализация и управление

- **`initializeBeeFlightSystem()`** - Инициализация системы
- **`startBeeFlight()`** - Запуск системы полета
- **`stopBeeFlight()`** - Остановка системы
- **`beeFlightLoop()`** - Основной игровой цикл
- **`monitorSolarEclipse()`** - Отслеживание затмения солнца
- **`updateAllBeesVisualEffects()`** - Обновление визуальных эффектов всех пчел

### Управление пчелами

- **`spawnBee()`** - Создание новой пчелы
- **`updateBees()`** - Обновление всех пчел
- **`removeBee(bee)`** - Удаление пчелы из системы
- **`clearAllBees()`** - Очистка всех пчел

### Логика полета

- **`updateFlyingBee(bee)`** - Логика летящей пчелы
- **`updateSittingBee(bee)`** - Логика пчелы на цветке
- **`updateReturningBee(bee)`** - Логика возвращающейся пчелы
- **`flyToTarget(bee)`** - Полет к цели
- **`setRandomTarget(bee)`** - Установка случайной цели

### Взаимодействие с цветками

- **`findNearestFlower(bee)`** - Поиск ближайшего цветка
- **`getFlowerColor(flower)`** - Получение цвета цветка

### Отслеживание солнца

- **`monitorSolarEclipse()`** - Отслеживание затмения солнца

## Визуальная реализация

### CSS стили

#### Основной элемент пчелы
```css
.bee-flight {
    position: absolute;
    width: 10px;
    height: 10px;
    background: #FFD700; /* Золотистый цвет */
    border-radius: 50%;
    border: 1px solid #000;
    transform-origin: center center;
    will-change: transform, left, top, background-color, box-shadow;
    transition: all 0.3s ease;
}
```

#### Анимация крыльев
```css
.bee-flight::before, .bee-flight::after {
    content: '';
    position: absolute;
    width: 4px;
    height: 2px;
    background: rgba(255, 255, 255, 0.8);
    border-radius: 50%;
    animation: bee-wing-left/right 0.1s ease-in-out infinite alternate;
}
```

#### Визуальные эффекты в зависимости от освещенности
```css
.bee-flight.eclipse-mode {
    /* При затмении - более тусклое свечение */
    box-shadow: 0 0 5px rgba(255, 215, 0, 0.4);
    filter: brightness(0.8);
}

.bee-flight.bright-sun-mode {
    /* При ярком солнце - яркое свечение */
    box-shadow: 0 0 12px rgba(255, 215, 0, 1);
    filter: brightness(1.2);
}
```

#### Эффекты взаимодействия
```css
.bee-flight:hover {
    box-shadow: 0 0 10px rgba(255, 215, 0, 0.8);
    transform: translate(-50%, -50%) scale(1.1);
}
```

### Цветовая схема цветков

- **Ромашка (daisy)**: `#FFFFFF` (белый)
- **Тюльпан (tulip)**: `#FF69B4` (розовый)
- **Мак (poppy)**: `#DC143C` (красный)
- **Василек (cornflower)**: `#4169E1` (синий)
- **Лаванда (lavender)`: `#9370DB` (фиолетовый)

## Интеграция с игрой

### Сезонная система

- **Весна и лето**: Система активна, пчелы летают
- **Осень и зима**: Система останавливается, пчелы исчезают

### Отслеживание затмения

Система автоматически отслеживает состояние солнца через существующий механизм `.eclipse-overlay` и изменяет как скорость спавна, так и скорость полета пчел:

```javascript
if (beeFlightSystem.eclipseActive) {
    beeFlightSystem.spawnInterval = 1000; // 1 пчела/сек при затмении
    beeFlightSystem.currentSpeedMultiplier = 0.5; // 50% от базовой скорости
} else {
    beeFlightSystem.spawnInterval = 333;  // 3 пчелы/сек при ярком солнце
    beeFlightSystem.currentSpeedMultiplier = 1.5; // 150% от базовой скорости
}
```

### Визуальные эффекты

При изменении освещенности система также применяет визуальные эффекты ко всем пчелам:

- **При затмении**: `.eclipse-mode` - тусклое свечение, пониженная яркость
- **При ярком солнце**: `.bright-sun-mode` - яркое свечение, повышенная яркость

Эти эффекты автоматически применяются ко всем существующим пчелам при изменении состояния солнца.

### Очистка ресурсов

При смене сезона система корректно очищает все ресурсы:
- Остановка игрового цикла
- Удаление всех DOM элементов пчел
- Очистка массива активных пчел

## Производительность

### Ограничения системы

- **Максимум пчел**: 20 одновременно активных пчел
- **Частота обновления**: 60 FPS (через requestAnimationFrame)
- **Проверка затмения**: каждые 100 миллисекунд

### Оптимизации

- **`will-change: transform, left, top, background-color`** - Уведомление браузера о предстоящих изменениях
- **Фильтрация массива пчел** - Автоматическое удаление завершивших полет пчел
- **Минимальные вычисления** - Эффективные математические операции для обнаружения цветков

## Логирование и отладка

### Консольные сообщения

Система предоставляет подробное логирование:

```javascript
console.log('=== Инициализация системы полета пчел ===');
console.log(`Спавн пчелы ${bee.id}, всего пчел: ${beeFlightSystem.bees.length}`);
console.log(`Пчела ${bee.id} села на цветок, цвет: ${bee.color}`);
console.log(`☀️ Состояние солнца: ${sunState}`);
console.log(`🐝 Скорость спавна: ${spawnRate} пчел/сек`);
console.log(`🚀 Скорость полета: ${speedPercent}% от базовой`);
```

### Мониторинг состояния

Доступны следующие метрики:
- Количество активных пчел
- Состояние системы (активна/неактивна)
- Состояние солнца (затмение/яркое)
- Интервал спавна пчел
- Скорость полета пчел (% от базовой)
- Визуальные эффекты (eclipse-mode/bright-sun-mode)
- Текущий множитель скорости

## Совместимость и требования

### Браузерная поддержка

- **Современные браузеры** с поддержкой ES6+
- **CSS3 анимации** и трансформации
- **requestAnimationFrame** API

### Зависимости

- **Отсутствуют внешние библиотеки**
- **Использует существующую DOM структуру**
- **Интегрируется с текущей системой сезонов**

## Будущие улучшения

### Возможные расширения

1. **Различные типы пчел**: рабочие пчелы, трутни, матка
2. **Погодные условия**: влияние дождя и ветра на полет
3. **Усталость пчел**: ограничение времени полета
4. **Коллекция нектара**: накопление нектара от цветков
5. **Визуальные эффекты**: частицы, следы полета

### Настройки производительности

- Динамическое изменение максимального количества пчел
- Адаптивная частота обновления в зависимости от производительности
- Качественные настройки для слабых устройств

---

**Дата создания**: 2024
**Версия**: 1.0
**Статус**: Реализовано и протестировано